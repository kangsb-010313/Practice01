package com.javaex.practice;

public class Ex03 {

	public static void main(String[] args) {
		
		/*
		 JVM에 대한 설명으로 틀린 것을 찾아 수정해보세요
		 
			(1) JVM은 java.exe 명령어에 의해 구동된다. (o)
			(2) JVM은 바이트코드(~.class)를 기계어로 변환시키고 구동시킨다. (o)
			
			(3) 운영체제별로 동일한 JVM이 사용된다. (x) 
				=> 각 운영체제에 맞게 만들어진 JVM이 따로 존재함. 리눅스, 맥, 윈도우 별로 전부 다름.
				
			(4) 바이트코드(~.class)는 운영체제에 독립적이만, JVM은 운영체제에 종속적이다. (o) 
				=> 바이트코드: 어떤 OS에서도 실행가능(단, 해당 OS에 맞는 JVM 필요)
				=> JVM: OS마다 별도로 구현되어야 함  
		 */
		
	}
	
}
